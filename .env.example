# Feature Flag System
# This application uses environment-based feature flags for opt-in functionality:
# - Authentication: Set AUTH_MODE to control authentication method (default: github)
# - Sentry: Provide NEXT_PUBLIC_SENTRY_DSN to enable error tracking
# - PostHog: Provide NEXT_PUBLIC_POSTHOG_KEY to enable analytics
# All tracking features are disabled by default unless the required environment variables are set.

# Convex Configuration
NEXT_PUBLIC_CONVEX_URL=http://127.0.0.1:3210  # For local development
CONVEX_DEPLOYMENT=your-convex-deployment-url   # For production

# AI API Keys (Required for AI responses)
ANTHROPIC_API_KEY=sk-ant-your-anthropic-api-key-here  # Required for Claude Sonnet 4 (default)
OPENAI_API_KEY=sk-your-openai-api-key-here            # Required for GPT models
OPENROUTER_API_KEY=sk-or-your-openrouter-key-here     # Required for additional AI models
EXA_API_KEY=your-exa-api-key-here                     # Required for web search functionality

# Authentication
AUTH_MODE=github                                      # Optional: 'github' (default), 'password', or 'google' (future)
AUTH_GITHUB_ID=your-github-oauth-client-id            # Required for GitHub authentication (default mode)
AUTH_GITHUB_SECRET=your-github-oauth-client-secret    # Required for GitHub authentication (default mode)
AUTH_GOOGLE_ID=your-google-oauth-client-id            # Required when AUTH_MODE='google' (future support)
AUTH_GOOGLE_SECRET=your-google-oauth-client-secret    # Required when AUTH_MODE='google' (future support)
JWT_PRIVATE_KEY=your-jwt-private-key                  # Required for API key encryption
JWKS=your-jwks-json-string                            # Required for JWT verification (JSON format)
ENCRYPTION_KEY=your-encryption-key                    # Optional fallback encryption key

# Convex Configuration (optional)
CONVEX_SITE_URL=https://your-site-url.com             # Optional for auth configuration

# Vercel Environment (optional)
NEXT_PUBLIC_VERCEL_ENV=development                     # deployment environment

# PostHog Analytics (optional - only enabled when key is provided)
NEXT_PUBLIC_POSTHOG_KEY=phc-your-posthog-project-key       # PostHog project API key
NEXT_PUBLIC_POSTHOG_HOST=https://us.posthog.com           # PostHog host URL

# Sentry Error Monitoring (optional - only enabled when DSN is provided)
NEXT_PUBLIC_SENTRY_DSN=https://your-sentry-dsn@sentry.io/project-id  # Sentry DSN for error tracking
NEXT_PUBLIC_SENTRY_ENVIRONMENT=development             # Environment name for Sentry (development/production)
SENTRY_ORG=lightfast                                  # Sentry organization (defaults to 'lightfast')
SENTRY_PROJECT=lightfast-chat                         # Sentry project (defaults to 'lightfast-chat')
SENTRY_AUTH_TOKEN=your-sentry-auth-token              # Optional: For source map uploads

# Node Environment
NODE_ENV=development
